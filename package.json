{
  "name": "grounded",
  "version": "1.0.0",
  "description": "Grounded project",
  "main": "index.js",
  "scripts": {
    "dev": "npx vite",
    "build": "npx vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "firebase": "^11.6.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "live-server": "^1.2.0",
    "vite": "^6.2.5"
  }
}
